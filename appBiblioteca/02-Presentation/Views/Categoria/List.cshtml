@model _02_Presentation.ViewModel.VMCategoria

@{
    ViewBag.title = "";
    ViewBag.subTitle = "";
}

<div class="row">
    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">Lista de Categorias</h3>
            </div>

            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tr>
                        <th class="col-md-10">Nombre</th>
                        <th>Acciones</th>
                    </tr>
                    @foreach (var categoria in Model.listaCategorias)
                    {
                        <tr>
                            <td class="fieldNombre">@categoria.nombre</td>
                            <td>
                                <a href="@Url.Action("Delete","Categoria", new { parameter = categoria.codigoCategoria })" onclick="return accionUsuario(this, 'Eliminar')" class="label label-danger">Eliminar</a>
                                <a href="@Url.Action("Edit","Categoria", new { parameter = categoria.codigoCategoria })" class="label label-info">Editar</a>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    function accionUsuario(element, accion) {
        var confirm = false;

        swal({
            title: '¿Estas Seguro?',
            text: accion + " la categoria.",
            type: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#39843A',
            cancelButtonColor: '#dd4b39',
            confirmButtonText: 'Si, ' + accion + '.'
        }).then(function () {
            window.location = $(element).attr('href');
        }, function (dismiss) {
            if (dismiss === 'cancel') {
                confirm = false;
            }
        });
        return confirm;
    }

</script>